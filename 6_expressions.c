#include <stdio.h>

int main(int argc, char const *argv[])
{
    /* code */

    /* Arithmetic + - * / % */ 

    int x, y, z, total;
    x = 2;
    y = 3;
    z = 2;
    total = 0;
   

    if(x == y) {
       total = x + y * z;
    }

    printf("Total %d", total);

    return 0;
}
